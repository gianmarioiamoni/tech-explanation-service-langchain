# Core LLM and prompting
langchain>=0.2.10,<0.3.0
langchain-openai>=0.1.8,<0.2.0
openai>=1.30.0,<2.0.0

# UI
gradio>=4.31.0,<5.0.0

# Hugging Face (explicit pin to avoid breaking changes)
huggingface_hub>=0.23.0,<0.25.0

# API (used locally and for future extensions)
fastapi>=0.111.0,<1.0.0
uvicorn>=0.30.0,<1.0.0

# Environment variables
python-dotenv>=1.0.1,<2.0.0

# Testing (not strictly required for Spaces, but good practice)
pytest>=8.2.0,<9.0.0
